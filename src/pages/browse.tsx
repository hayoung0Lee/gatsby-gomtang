import React, { useContext } from "react";
import Layout from "../components/layout";

const BrowsePage = () => {
  return (
    <Layout>
      <div>Browse Page </div>
    </Layout>
  );
};

export default BrowsePage;
